[package]
name = "neort-blocks"
version = "0.1.0"
edition = "2021"

[dependencies]
rtsan = { workspace = true }

[features]
default = ["alloc"]
alloc = []
rtsan = ["rtsan/enable"]

[dev-dependencies]
criterion = { workspace = true }

[[bench]]
name = "blocks"
harness = false
